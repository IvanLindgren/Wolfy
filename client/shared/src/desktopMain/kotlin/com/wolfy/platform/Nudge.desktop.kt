package com.wolfy.platform

/**
 * На Windows обновлять нечего.
 *
 * Панель «вас ждёт книга» живёт в том же процессе, что и приложение, и берёт
 * состояние из того же хранилища при каждом показе. Толчок извне ей не нужен.
 */
actual fun refreshBookNudge() = Unit

package com.wolfy.platform

/**
 * Просит систему обновить приглашение к книге за пределами приложения.
 *
 * На Android это виджет рабочего стола: система обновляет его сама не чаще
 * получаса, и без такого толчка он показывал бы вчерашнее место сразу после
 * чтения. На Windows панель живёт в том же процессе и следит за состоянием
 * сама — там делать нечего.
 *
 * Зовётся оттуда, где место в книге меняется, и только оттуда: приглашение,
 * которое обновляется на каждый кадр прокрутки, — это разряженная батарея.
 */
expect fun refreshBookNudge()

//! Библиотека пользователя: книги, полки и прогресс чтения.
//!
//! Здесь состояние и правила его изменения — но не хранилище. Где лежит файл
//! библиотеки и как он попадает на диск, знает клиент: на Android это одно
//! место, на настольной машине другое, а правила слияния с сервером обязаны
//! быть одни. Две реализации этих правил разойдутся тихо — прочитанная глава
//! просто исчезнет на одном из устройств.
//!
//! Времени и случайности у ядра своих нет: «сейчас» и новые номера приходят
//! параметрами. Из-за этого любое слияние проигрывается тестом целиком.

pub mod book;
pub mod merge;

pub use book::{LibraryBook, LibraryState, Progress, Shelf};
pub use merge::{apply_server, migrate, Sent};
